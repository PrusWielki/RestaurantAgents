To run the app:
- Check whether JADE library is added to libraries in project structure
- To start the program navigate to Main file and run the main class
- Restaurants preferences can be changed in the Main class in CreateAgents method, new restaurants can also be add in gui, same goes for clients
- Program was tested with JDK 18